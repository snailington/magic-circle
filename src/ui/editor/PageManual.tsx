const sourceTypes = [
    {
        name: "websocket",
        description: ""
    }
];

export default function PageManual({setPage}: {setPage: (page: string)=>void}) {
    return (
        <>
            <h1>Manual Setup</h1>
            <select>

            </select>
        </>
        );
}